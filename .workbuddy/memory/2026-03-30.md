# 2026-03-30 Daily Log

## [09:40] 创建记忆力训练游戏
- 为空 GitHub 仓库 https://github.com/xuyu3hen/game 从头创建完整记忆力训练游戏
- 两种游戏模式：
  - 翻牌配对（Memory Cards）：三种难度 简单(4×4) / 中等(4×5) / 困难(5×6)
  - 序列记忆（Simon Says）：无限关卡挑战
- 文件：index.html, style.css, game.js, README.md
- 深色主题 UI，3D 翻牌动画，localStorage 保存最佳成绩
- 代码已 commit 并成功 push 到 origin/master

## [09:51] 添加 GitHub Pages 自动部署
- 创建 .github/workflows/deploy.yml 配置 GitHub Actions 自动部署
- 每次推送代码到 master 分支时自动部署到 GitHub Pages
- 用户需在 GitHub Settings → Pages 中选择 Source 为 "GitHub Actions"
- 部署后游戏可通过 https://xuyu3hen.github.io/game/ 访问

## [10:02] 修复翻牌游戏底牌可见问题
- 问题：翻牌游戏开始时卡牌正面 emoji 直接可见
- 修复：将 card-front 初始 opacity 设为 0，翻转后设为 1
- 使用 opacity 替代 backface-visibility 确保跨浏览器兼容
- 修复已 commit 并 push 到 origin/master，Actions 自动重新部署

## [10:05] 添加玩家 ID 系统，不同浏览器有独立成绩
- 实现 getPlayerId() 函数：首次访问时生成唯一 ID（存储在 localStorage）
- 成绩 key 改为 `{playerId}-{game}-{level}` 格式，实现玩家隔离
- 首页显示玩家 ID（截取后8位），方便区分不同浏览器
- 不同浏览器 / 隐私模式 / 清除缓存 = 新玩家 ID，各自独立记录成绩
- 代码已 commit 并 push，Actions 自动部署

## [10:15] 添加 Supabase 后端排行榜系统
- 创建 supabase-schema.sql：游戏记录表 + RLS 策略 + 排行榜视图
- 前端接入 Supabase SDK：引入 CDN，初始化客户端
- 首页新增：
  - 排行榜区域（4 个标签：翻牌简单/中等/困难 + 序列记忆）
  - 昵称输入框（可选，显示在排行榜）
  - 自动加载并渲染排行榜数据
- 游戏完成时自动提交成绩到 Supabase
- 玩家自己的记录在排行榜中高亮显示
- 待完成：用户提供 Supabase anon key 后替换 game.js 中的占位符
- 代码已 commit 并 push，需用户提供 anon key 并执行 SQL 才能启用

## [10:20] 配置 Supabase anon key
- 用户提供 Supabase anon key（JWT token）
- 已替换 game.js 中的占位符，Supabase 客户端初始化完成
- 代码 commit 并 push，Actions 自动部署
- 待用户执行 supabase-schema.sql 导入数据库表结构

## [10:25] 修复游戏无法启动的 bug
- 问题： getPlayerId() 函数在声明前被调用导致 ReferenceError
- 修复：将 getPlayerId 函数定义移到 gameState 初始化之前
- 删除了重复的函数定义
- 代码已修复并 push，游戏恢复正常

## [10:29] 修复翻牌游戏无法进入界面
- 问题：Supabase SDK 在 <head> 中加载阻塞了后续脚本执行
- 错误：`Uncaught ReferenceError: startGame is not defined`
- 修复：将 Supabase SDK 移到 <body> 开头
- 添加了调试日志帮助定位问题
- 代码已修复并 push，游戏恢复正常

